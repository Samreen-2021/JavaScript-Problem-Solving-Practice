// Print the first and last member of the same array
let games = ["football", "tennis", "rugby", "hockey"];


    console.log("first element of array is " + games[0]);
    console.log("last element of array is " + games[(games.length-1)]);
