// Traverse the array using a for loop
// Print its members and index using the same for loop

let games = ["football", "tennis", "rugby", "hockey"];

for (let i =0; i<games.length;i++){
    console.log(games[i] + " it's index is "+ i);
}